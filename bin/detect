#!/usr/bin/env bash

#if [ -f $1/build.xml ] && grep -q "target.*name=[\'|\"]stage[\'|\"]" $1/build.xml ; then
#  echo "Custom PHP" && exit 0
#else
#  echo "no" && exit 1
#fi

if [ -f $1/index.php || -f $1/composer.json ]; then
  echo "Custom PHP" && exit 0
else
  echo "no" && exit 1
fi

